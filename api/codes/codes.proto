syntax = "proto3";

option go_package = "github.com/dstreamcloud/dt-proto/pbgo/codes;codes_pb";

package codes;

enum Error {
    UNKNOWN = 0;
    INVALID_REQUEST = 400;
    UNAUTHORIZED = 401;
    FORBIDDEN = 403;
    NOT_FOUND = 404;
}
